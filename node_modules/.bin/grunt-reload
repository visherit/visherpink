#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-reload/bin/grunt-reload" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-reload/bin/grunt-reload" "$@"
  ret=$?
fi
exit $ret
