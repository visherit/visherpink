git status
git add .
git commit -m "firs comit"

git push -u origin master
----------------------------------
grunt sass
Все стили подтянуть импортами
npm install -g browser-sync - обноврелние в реальном времени.
browser-sync start --server --files "**/**/*.scss" - запуск сервера для реального времени.